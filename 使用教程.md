# easyol · 使用教程

一套基于**飞书多维表格 + 飞书邮件**的 KOL 联动管理台。每个人在自己电脑上本地运行，用自己的飞书账号、对着自己的表格工作，浏览器访问 `localhost` 使用。

> 工作原理：本地的 Node 服务通过你本机的 `lark-cli`（以你登录的飞书身份）读写飞书表格和邮件，并调用本机的 `claude` 或 `codex` 生成邮件草稿、分析账号。**没有中心服务器，不收集任何人的账号信息**——你的飞书登录只存在你自己电脑的 lark-cli 里。

---

## 一、准备工作（每台电脑装一次）

| 工具 | 用途 | 是否必装 |
| --- | --- | --- |
| **Node.js** ≥ 18（建议 20+） | 运行本服务 | ✅ 必装 |
| **lark-cli** | 读写飞书表格与邮件 | ✅ 必装（且要登录） |
| **claude** 或 **codex** | 生成邮件草稿 / 账号分析 | 二选一即可，装了都能用 |

检查是否装好（终端运行）：

```bash
node -v
lark-cli --version
claude --version   # 或 codex --version
```

---

## 二、登录飞书（每个人用自己的账号）

在终端运行下面命令，会弹出授权链接 / 二维码，按提示完成登录：

```bash
lark-cli auth login --scope "bitable:app drive:drive mail:user_mailbox.message mail:user_mailbox.message:send"
```

- 这一步是**一次性**的，登录后身份保存在本机。
- 如果之后使用中提示「缺少某个 scope / authorization」，按报错里的 `missing_scope` 再 `login` 一次即可，权限会累积。

---

## 三、启动与配置

```bash
# 拉取代码（仅首次）
git clone <仓库地址> easyol && cd easyol

# 启动（本项目零依赖，无需 npm install）
npm start
```

启动后浏览器打开终端提示的地址（默认 <http://localhost:4173>）。**配置有两种方式，任选其一：**

### 方式 A：网页配置（推荐）

首次未配置时，打开页面会自动进入**配置向导**：

1. **① 依赖检测**：确认 lark-cli / claude / codex 状态。
2. **② 飞书登录**：点命令框可一键复制登录命令，去终端执行（见第二节）。
3. **③ 填写表格与身份**：
   - 把浏览器里打开你多维表格的**完整 URL 粘贴**到第一栏，会自动解析出 base token / table id / view id；
   - 填 **OWNER_EMAIL**（你的飞书邮箱，用来区分邮件里的“我方”和“KOL”）；
   - 选 **团队品牌**（Bloome / Renoise）。
4. 点 **「保存并继续」**：系统会写入配置并自动验证飞书连接，通过后直接进入主界面（**无需重启**）。
   - 若提示“未登录/授权”，去终端跑登录命令，再点「我已登录，重新检测」。

> 想以后重新配置：随时访问 `http://localhost:4173/setup.html`。

### 方式 B：命令行配置

```bash
npm run setup
```

按提示交互式填写（同样支持粘贴飞书 URL 自动解析），生成 `.env` 后再 `npm start`。

---

## 四、如何获取 base token / table / view

在浏览器打开你的多维表格，复制地址栏 URL：

- 常规链接形如：`https://<域名>/base/<这一段就是 base token>?table=<table id>&view=<view id>`
- 配置向导粘贴这条 URL 就能自动解析；解析不到的（如知识库内嵌的表）手动补填即可。

---

## 五、表格字段要求 ⚠️

系统按**固定的中文字段名**读取表格，**字段名不一致会导致读不出数据**。建议从团队提供的**模板表复制一份**。需要的字段：

`主页URL`、`频道ID`、`标签`、`联系方式`、`负责人`、`重复`、`资源来源`、`语种`、`合作形式`、`联系状态`、`国家`、`备注（报价、合作形式等）`、`受众截图`、`状态更新时间`、`添加时间`

---

## 六、界面与日常操作

### 顶部导航栏
- **打开飞书表格**：跳转到你配置的表格链接。
- **搜索框**：按频道名、邮箱、状态、备注等模糊搜索。
  - 快捷键 **⌘F / Ctrl+F** 直接聚焦搜索框。
  - 搜索命中**唯一结果**时会**自动打开**那个 KOL，无需再点。
- **状态筛选标签**（全部 / 已联系 / 沟通中 / …）：点一下只看该状态；右上角数字是各状态数量。
- **刷新按钮**（圆形图标）：重新从飞书拉取最新数据，刷新时图标会转动。

### 左侧 KOL 列表
- 每张卡片显示频道名、状态、邮箱和语种/合作等信息。点击即在右侧打开详情；当前选中项左侧有玫瑰色竖条标记。

### 右侧详情面板
- **头部**：频道名、邮箱，右上角是**状态下拉**（点击可直接改联系状态，自动写回飞书）。
- **操作按钮**：打开主页、打开飞书、**分析账号**（调用 codex 分析该频道）、复制邮箱。
- **信息行**：标签 / 负责人 / 语种 / 合作 / 来源 / 更新时间，以及**备注**：
  - 备注是一个紧凑胶囊，**点击它任意位置即可编辑**；
  - 内容过长会省略号截断，**鼠标悬停弹出完整内容**；
  - 编辑后点保存写回飞书。
- **邮件往来**：自动按当前 KOL 的邮箱/频道名搜索飞书邮件，按会话气泡展示「我方 / KOL」。
  - 点击任意邮件气泡可弹窗**查看完整邮件正文**（富文本/HTML）。

### 生成回复草稿
1. 在底部输入框写下你的回复意图（例如：“接受他的报价，询问档期”）。
2. 右侧切换 **Claude / Codex**（用哪个 AI 生成）。
3. 点 **「生成草稿」**：会结合该 KOL 的表格状态 + 邮件历史，生成一封英文回复草稿并预览。
4. 预览区可：
   - **通过，创建飞书草稿**：在你的飞书邮箱里创建草稿（或对最近来信回复），并给出查看链接；
   - **复制文本** / **关闭**。

> 草稿只会“创建草稿/预览”，需要你人工确认后再发送；AI 不会自动发邮件。

---

## 七、多人使用

每个人在自己电脑上重复「准备工作 → 登录飞书 → 启动并配置」即可：

- 各自用**自己的飞书账号**和**自己的表格**，数据互不干扰；
- 邮件里的“我方/KOL”按各自的 `OWNER_EMAIL` 判定；
- 团队品牌、AI 工具等都各自配置。

---

## 八、配置项速查

| 变量 | 必填 | 说明 |
| --- | --- | --- |
| `LARK_BASE_TOKEN` | ✅ | 多维表格 base token |
| `LARK_TABLE_ID` | ✅ | 数据表 table id |
| `LARK_VIEW_ID` | ✅ | 视图 view id |
| `FEISHU_TABLE_URL` | 建议 | “打开飞书表格”按钮跳转链接 |
| `OWNER_EMAIL` | ✅ | 你的飞书邮箱，区分邮件我方/对方，可逗号分隔多个 |
| `BRAND` | 否 | 团队品牌（`Bloome` / `Renoise`），用于邮件主题与 AI 提示词，默认 `Bloome` |
| `PORT` | 否 | 本地端口，默认 `4173`（改动需重启） |
| `CLAUDE_BIN` / `CODEX_BIN` | 否 | AI 可执行路径，默认从 PATH 找 |
| `FEISHU_MAIL_URL` | 否 | 创建草稿后“在飞书中查看”的回退链接 |

> 配置保存在项目根目录的 `.env`，已被 `.gitignore`，**不要提交**。

---

## 九、常见问题

| 现象 | 原因 / 解决 |
| --- | --- |
| 打开页面是“配置向导” | 还没配置，按向导填写即可 |
| 读表/邮件报授权错误（missing scope / authorization） | 补登录：`lark-cli auth login --scope "<缺的 scope>"`，然后刷新 / 点重新检测 |
| 页面没数据或字段为空 | 多半是表格字段名与第五节不一致，用模板表复制 |
| 生成草稿提示“没有找到 claude/codex 命令” | 对应工具没装或路径不对，装好后在配置里填 `CLAUDE_BIN` / `CODEX_BIN` |
| 改了端口不生效 | 端口改动需要重启 `npm start` |
| 别的设备访问不了 | 服务只监听本机 `127.0.0.1`，请在本机浏览器访问 |
